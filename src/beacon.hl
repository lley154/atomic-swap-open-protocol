minting beacon

enum Redeemer { 
    Mint
    Burn 
}

// Beacon minting script parameters
const APP_WALLET_PKH: ByteArray = #
const appWalletPkh: PubKeyHash = PubKeyHash::new(APP_WALLET_PKH)

func main(_, ctx: ScriptContext) -> Bool {
    ctx.tx.is_signed_by(appWalletPkh).trace("BTP1: ")
}