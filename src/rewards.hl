minting rewards

enum Redeemer { 
    Mint
    Burn 
}

// Contract parameters 
const VERSION: ByteArray = # 
const OWNER_PKH: ByteArray = #
const ownerPkh: PubKeyHash = PubKeyHash::new(OWNER_PKH)

func main(redeemer: Redeemer, ctx: ScriptContext) -> Bool {

    redeemer.switch {
        Mint => {
            ctx.tx.is_signed_by(ownerPkh).trace("RP1: ")
        },
        Burn => {
            true
        }
    }
}