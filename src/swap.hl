spending swap


struct Datum {
    askedAsset: Int
    offeredAsset: Int
}

enum Redeemer {
    Swap
    Close { 
        pkhBA: ByteArray 
    }
}

func main(_, _, _) -> Bool {
    print("swap contract");
    true
}