spending userTokenValidator

enum Redeemer {
    Mint
    Burn
}

// Escrow validator script parameters 
const APP_PKH: ByteArray = #


func main(_, _, _) -> Bool {
    print("userTokenValidator");
    true
}